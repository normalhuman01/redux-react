import Card from './Card';

const Suma = props => {
    return (
        <Card title="Suma de números" blue>
            <div>
                <span>
                    <span>Resultado: </span>
                    <strong>{10}</strong>
                </span>
            </div>
        </Card>
    )
}

export default Soma;